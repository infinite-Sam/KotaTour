<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="BoardDAO">

	<!-- 게시글 목록 조회 -->
	<select id="getBoardList" parameterType="String" resultType="board">
		SELECT * FROM board
		ORDER BY boardNum DESC
	</select>

	<!-- 게시글 상세보기 -->
	<select id="getBoardDetail" parameterType="int" resultType="Board">
		SELECT * FROM board
			WHERE boardNum=#{boardNum}
	</select>
	
	<!-- 게시글 등록 --> 
	<insert id="insertBoard">
		INSERT INTO board(boardNum, title, content, name, indate)
		VALUES (board_seq.NEXTVAL, #{title}, #{content}, #{name}, TO_char(sysdate, 'yyyy.mm.dd hh24:mi'))
	</insert>
	
	<!-- 게시글 삭제 -->
	<delete id="deleteBoard">
		delete from board
			where boardNum=#{boardNum}
	</delete>
	
	<!-- 게시글 수정 -->
	<update id="updateBoard">
		update board set 
			title=#{title}, content=#{content}, indate=TO_char(sysdate, 'yyyy.mm.dd hh24:mi')
			where boardNum=#{boardNum}
	</update>
</mapper>
